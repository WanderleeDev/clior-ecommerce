model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  name         String
  createdAt    DateTime @default(now())

  addresses Address[]
  orders  Order[]
  reviews Review[]

  @@map("users")
}
